# File Name: type2.py

# Description: This script simply prints the type of an empty list.

# Print the type of an empty list using the 'type' function.
print(type([]))

# Explanation:

# Description: This script simply prints the type of an empty list.

# The script consists of a single line of code:

# - 'print(type([]))': This line uses the 'type' function to determine the type (class) of an empty list (represented by '[]') and then prints the result.
#   In this case, it will print the type 'list' to the console, indicating that the value is a list.
